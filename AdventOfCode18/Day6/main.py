import re
from collections import defaultdict

import Readfile as rf

def functions(i,j)

def part1(data):
    corr = map(lambda s: map(int, re.findall(r'-?\d+', s)), data)
    points = defaultdict(list)

    for i in range(1000):
        best = None
        for j in range(1000):
            pass
    for (x,y) in corr:

        pass


def part2(data):
    pass


if __name__ == '__main__':
    data = rf.read_file('input')
    print(part1(data))
    print(part2(data))
